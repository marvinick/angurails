<div class="container">
  <div class="row">
    <div class="col-md-12">
      <% @pictures.each do |picture| %>
        <%= picture.title %>
        <%= image_tag picture.image_url.to_s %>
      <% end %>
    </div>
  </div>
</div>