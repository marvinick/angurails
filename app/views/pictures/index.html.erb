<div class="container">
  <div class="row">
    <% @pictures.each do |picture| %>
      <div class="col-md-6">
        <%= picture.title %>
        <%= image_tag picture.image_url(:thumb).to_s %>
      </div>
    <% end %>
  </div>
</div>